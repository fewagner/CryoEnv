from setuptools import setup

setup(name='cryoenv',
      version='0.0.1',
      install_requires=['gym',
                        'stable_baselines3',
                        'tqdm',
                        'numba',
                        ]
      )
